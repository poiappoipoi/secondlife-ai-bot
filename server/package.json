{
  "name": "secondlife-ai-bot",
  "version": "2.0.0",
  "description": "AI bot server for Second Life with multi-provider support",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun --typecheck src/index.ts && bun src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "bun --typecheck src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
